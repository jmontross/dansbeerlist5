<info2>
	<ul>
	<% if current_user.microposts.count + current_user.following.count +  current_user.followers.count +  current_user.posts.count + current_user.comments.count + current_user.participations.count > 100%>
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Blackedout...<br/>
	<% else %>
	<% if current_user.microposts.count + current_user.following.count +  current_user.followers.count +  current_user.posts.count + current_user.comments.count + current_user.participations.count > 50%>
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Smashed...<br/>
	<% else %>
	<% if current_user.microposts.count + current_user.following.count +  current_user.followers.count +  current_user.posts.count + current_user.comments.count + current_user.participations.count > 20%>
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Wasted...<br/>
	<% else %>
	<% if current_user.microposts.count + current_user.following.count +  current_user.followers.count +  current_user.posts.count + current_user.comments.count + current_user.participations.count > 10%> 
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Drunk...<br/>
	<% else %>
	<% if current_user.microposts.count + current_user.following.count +  current_user.followers.count +  current_user.posts.count + current_user.comments.count + current_user.participations.count > 5%> 
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Buzzed...<br/>
	<% else %>
	<li><u><%= link_to "Drinker Level", beerbadges_path %></u>: You're Sober...<br/>
     <% end %>
     <% end %>
     <% end %>
     <% end %>
     <% end %>
     </ul>
    </info2>