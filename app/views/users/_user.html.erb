<li>
 	<% if user.gravatar_file_name %>
     <%= link_to image_tag(user.gravatar, :width => '150'), user %>
     <% else %>
        	<%= link_to default, user %>
				<% end %>
    
     <%= link_to user.username, user %>

     <% if current_user.admin? %>
	  | #/destroy | <%= user.email %>
	  <% end %>
</li>